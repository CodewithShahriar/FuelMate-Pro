# Supabase (optional — app runs in mock mode without these)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Also supported (Vite naming) — set these if exporting to a Vite environment
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=